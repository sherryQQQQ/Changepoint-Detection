# 结构突变、变点检测与状态建模：跨领域选择性综述

## 1. 三种相似、但不相同的问题

一个时间序列出现变化，至少可以提出三个问题：回归系数是否稳定，生成分布何时改变，以及当前属于哪个潜在状态。结构突变文献侧重参数稳定性与统计推断；变点检测侧重时间分段；状态切换模型侧重状态、转移规律和预测。这里的分类是本文的组织框架，并非互斥定义。[Casini & Perron, 2018](https://arxiv.org/abs/1805.03807)；[Truong et al., 2020](https://doi.org/10.1016/j.sigpro.2019.107299)；[Hamilton, 2016](https://www.nber.org/papers/w21863)。

| 维度 | Structural breaks | Change-point detection | Regime switching |
|---|---|---|---|
| 典型对象 | 回归系数及误差结构 | 均值、方差或更一般分布 | 潜在状态和状态相关参数 |
| 典型输出 | 检验、断点估计与区间 | 断点集合或报警时间 | 状态概率、转移概率与预测 |
| 重复状态 | 通常不强制跨段共享参数 | 基础分段通常不共享 | 有限状态模型可反复访问同一状态 |
| 信息集 | 常见为完整历史样本 | 离线与在线两大类 | 可做过滤，也可做平滑 |
| 主要风险 | 错误推断或遗漏断点 | 过度分段、误报或延迟 | 状态数误设、识别弱、局部最优 |

例子：序列先低波动、再高波动、最后回到低波动。分段方法可以找到两个边界，但不自动认定第一段和第三段共享参数；两状态模型则明确允许这种共享。反过来，如果参数每次改变后都取新值，固定有限状态模型可能不断增加状态才能拟合。

## 2. 一个用于比较的表示

令观测为 $y_t$，协变量为 $x_t$，历史信息为 $\mathcal F_{t-1}$。用下式作为比较不同模型的记号：

$$
y_t\mid x_t,\mathcal F_{t-1},z_t\sim p(y_t\mid x_t,\mathcal F_{t-1};\theta_{z_t}).
$$

这是教学性表示，不意味着所有方法都需要指定完整似然。不同方法对 $z_t$ 和参数共享施加不同约束。

**分段模型。** 给定 $0=\tau_0<\tau_1<\cdots<\tau_K<\tau_{K+1}=T$，在第 $j$ 段令 $z_t=j$。一种典型的估计目标是：

$$
\min_{K,\tau}\left\{\sum_{j=0}^{K} C(y_{\tau_j+1:\tau_{j+1}})+\lambda K\right\}.
$$

代价函数决定“什么变化值得检测”，搜索算法决定“怎样求解”，惩罚或约束决定分段复杂度。这一组织方式来自 [Truong et al. (2020)](https://doi.org/10.1016/j.sigpro.2019.107299)。仅更换搜索算法无法修复错误的代价函数：均值变化损失未必能可靠识别纯方差变化。

**Markov 状态模型。** 令 $z_t\in\{1,\ldots,M\}$，并设置 $P_{ij}=\Pr(z_t=j\mid z_{t-1}=i)$。同一状态被再次访问时，重复使用其参数。历史重建与实时使用要分开：平滑概率 $\Pr(z_t\mid y_{1:T})$ 使用未来观测，过滤概率 $\Pr(z_t\mid y_{1:t})$ 仅使用当前及过去观测。实时应用还必须保证参数估计也只使用当时可用数据。[Hamilton, 2016](https://www.nber.org/papers/w21863)。

## 3. 结构突变：推断对象是参数稳定性

Bai–Perron 框架的重要性在于把多个未知断点、分段回归和统计推断放在同一个问题中。其研究不能简单等同于“在曲线上找拐点”：协变量关系可能已经改变，而序列的边际均值仍然接近不变。[Bai & Perron, 1998](https://doi.org/10.2307/2998540)。

实际应用需要明确哪些系数允许变化、最短分段长度以及误差的依赖和异方差结构。Bai & Perron (2003) 讨论动态规划、断点置信区间与多断点检验，为实现提供更直接的入口。[原文](https://doi.org/10.1002/jae.659)。

这一文献线适合研究“模型关系是否稳定”，但检测到断点本身不识别因果机制。将断点和某个政策或新闻日期对应起来，可以形成后续研究假设，不能单凭时间接近给出因果结论。

## 4. 离线变点：代价函数与搜索算法应分开

PELT 是惩罚分段目标的搜索方法。它通过剪枝减少候选集合；在满足相应条件时具有线性期望计算成本，不能把标题中的 linear 理解为对任意数据、损失和配置都有最坏情形线性保证。其“精确”也指对所设目标和允许的分段集合求解，不保证该目标正确描述数据。[Killick, Fearnhead & Eckley, 2012](https://doi.org/10.1080/01621459.2012.737745)。

离线方法适合历史数据分析。若把完整样本的断点直接用于历史交易决策，会改变当时的信息集。一个算法能反复在前缀样本上运行，不代表它自动具有在线误报控制或可接受的实时成本。

## 5. 在线检测：把误报与延迟一起报告

在线检测选择报警时刻 $\mathcal T$，并要求决策只能依赖当时的信息。典型目标是在误报约束下缩短变化后的检测延迟；具体约束可以是无变化时平均报警时间或贝叶斯误报概率，不同准则不能混为一谈。[Xie et al., 2021](https://arxiv.org/abs/2104.04186)。

BOCPD 跟踪自上次变化以来的 run length 后验，用 hazard 描述变化先验，并结合段内预测分布递推。它回答的是最近一次变化的位置不确定性；基础模型不会自动为不同历史段学习可复用的状态身份。[Adams & MacKay, 2007](https://arxiv.org/abs/0710.3742)。

实践中应事先定义如何从后验产生报警、报警后如何重置及如何合并重复报警。后验图本身不是完整的在线检测系统，也不能替代误报校准。

## 6. 状态切换：复用信息，也引入额外假设

Hamilton (1989) 是经济时间序列潜在状态切换的代表性原始论文。模型利用隐状态解释增长率的离散变化，并通过状态推断与似然估计连接观测和经济状态。[论文](https://doi.org/10.2307/1912559)；[作者资料与数据入口](https://econweb.ucsd.edu/~jhamilto/)。

由有限状态齐次 Markov 假设可直接推得，状态 $i$ 的连续停留长度服从几何分布：$\Pr(D=d)=P_{ii}^{d-1}(1-P_{ii})$。这给出了方便的持续性表示，但也限制了持续时间形状。如果研究对象具有明显的持续时间依赖，显式持续时间模型值得进一步考察；本稿不展开其完整文献。

状态名称也不是模型自动赋予的经济事实。无监督拟合得到的“状态 1”需要结合参数、样本外表现和领域信息解释；不同拟合之间的标签排列可能不同。

## 7. 从检测到适应：连接 concept drift

检测到变化之后，还需要决定是否更新模型、采用什么窗口、是否重用旧模型。在监督学习中，特征分布变化与条件关系变化也应区分：$p_t(x)$ 改变不必然意味着 $p_t(y\mid x)$ 改变。Gama 等人的综述把漂移、适应策略和评价联系起来，为这一步提供入口。[Gama et al., 2014](https://doi.org/10.1145/2523813)。

本文据此提出一个实践判断：检测系统的价值应通过最终任务验证。检测更敏感可能带来更多无效重训；识别出可复用状态可能节省数据，但错误复用也可能加重模型偏差。这些是待实证比较的权衡，不是已得到的实验结论。

## 8. 如何公平比较

| 目标 | 应报告什么 | 不能替代它的指标 |
|---|---|---|
| 历史断点定位 | 一对一容差匹配的 precision/recall、定位误差 | 单纯样本内拟合优度 |
| 在线报警 | 误报行为、检测延迟、漏检率 | 使用全样本的边界 F1 |
| 状态恢复 | 标签匹配后的准确率或 ARI、持续时间恢复 | 未处理标签置换的准确率 |
| 预测 | 按时间滚动的误差或预测密度评分 | 全样本估计后的回填预测 |
| 推断 | 覆盖率、区间长度或检验大小 | 点估计的视觉接近程度 |

这张表是本项目提出的评价组织方案。不同方法只有在共同目标和相同信息集下才适合排序。例如 PELT 和 HMM 可以都产生边界，但前者的段数与后者的可复用状态数不是同一个量，不应强制设成相等。

## 9. 值得在 GitHub 上进一步回答的问题

1. **重复性何时值得建模？** 在相同信噪比下，比较一次性参数变化与重复状态，观察参数共享是否改善预测。
2. **模型误设会留下什么症状？** 用渐变、异常值和重尾噪声检验过度分段、状态膨胀与误报。
3. **状态体系本身变化怎么办？** 把“已知状态之间切换”和“出现全新状态”区分开，作为后续扩展。

这些问题是项目选题，不是对文献空白或方法创新性的断言。第一版实验协议见 [benchmark-plan.md](benchmark-plan.md)。
