# 最小实验协议（尚未执行）

目标：检验“断点定位”与“重复状态建模”的取舍；不寻找适用于所有场景的总冠军。

## 数据生成

每种场景使用长度 T=2000 的序列和 50 个固定随机种子。保存生成参数、真实边界与状态标签。

| 场景 | 生成方案 | 要检验的问题 |
|---|---|---|
| 一次性均值变化 | 每 400 点换均值，均值依次为 0、1、2、3、4；标准差 1 | 独立新段是否比固定两状态更合适 |
| 重复状态 | 两状态 Markov 链，P 的对角元为 0.98，均值 0 与 2，标准差 1 | 共享历史状态参数是否有帮助 |
| 方差变化 | 均值 0，标准差每 400 点在 1 与 3 之间切换 | 均值损失误设会怎样 |
| 缓慢漂移 | 均值从 0 线性增加至 2，标准差 1 | 突变模型会如何离散化连续变化 |
| 稳定但有异常值 | 均值 0、标准差 1，随机 1% 观测额外加 ±8 | 对异常值的敏感性 |

独立生成校准序列。逐项变化信噪比、平均持续时间和噪声分布，避免一次同时改变所有困难因素。渐变场景没有天然的离散真断点，不给它计算“真实断点 F1”。

## 方法与任务

- 离线：惩罚最小二乘分段、PELT（均值损失和方差敏感损失）、Gaussian HMM。先只比较历史重建；HMM 同时报告状态数与推导出的边界数。
- 在线：CUSUM 与 BOCPD，校准至相近的无变化误报水平后比较延迟；明确报警阈值与重置规则。
- 预测：静态模型、滚动窗口模型、仅用历史样本拟合的状态模型。断点方法若参与，需完整定义分段后如何拟合预测器。

Bai–Perron 的系数稳定性推断放入后续回归场景；第一版不把简单均值分段冒充完整 Bai–Perron 复现。

## 评价与信息集

1. 离线边界采用 ±10 点容差的一对一匹配，并以 ±5、±20 做敏感性分析。未匹配真边界计漏检；未匹配估计边界计误检。
2. 状态恢复报告 ARI；重复状态之外的场景不强求状态标签指标。HMM 至少 10 次初始化，记录收敛和选择规则。
3. 在线报告无变化序列的平均报警时间、有限观察窗内报警比例，以及变化后的延迟和漏检。未报警序列按删失处理，不能简单丢弃。
4. 预测采用时间顺序训练/验证/测试（50%/25%/25%），测试中允许按预设周期只用过去数据重训。缩放、阈值与状态数选择均不得使用测试未来信息。
5. 报告跨种子的均值、标准差、运行时间及失败次数。误差条反映模拟重复性，不自动构成理论置信区间。

## 完成标准

后续代码需保存环境版本、配置、随机种子和原始指标，并提供一个可复现入口。第一张图显示观测、真实变化、估计边界与过滤状态概率；第二张图比较误报—延迟权衡。结果解释必须包含失败案例。当前没有代码、性能数值或已完成复现的声明。
